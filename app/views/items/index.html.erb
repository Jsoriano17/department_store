<h1><%= "#{@department.name}"%> Department</h1>
<% @items.each do |item| %>
  <h2><%= item.name %></h2>
  <%= link_to 'Item price', department_items_path(item) %>
  <br>
  <%= link_to 'Edit Item', edit_department_path(item) %>
  <br>
  
<% end %>

<h1>Create New Item</h1>
  <%= link_to 'Create New Item', new_department_item_path %>
  <br>
   <br>
    <%= link_to 'Back', departments_path %>