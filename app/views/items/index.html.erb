<h1><%= "#{@department.name}"%> Department</h1>
<% @items.each do |item| %>
  <h2><%= item.name %></h2>
  <%= link_to 'Item Info', department_item_path(@department, item) %>
    <%= link_to 'Edit item', edit_department_item_path(@department, item) %>
   <%= link_to 'Delete Item', [@department, item], method: :delete %>
<% end %>

<h1>Create New Item</h1>
  <%= link_to 'Create New Item', new_department_item_path %>
  <br>
   <br>
    <%= link_to 'Back', departments_path %>

 